export const SET_TOKEN = (state, token) => {
    state.token = token
}

export const SET_ROUTES = (state, routes) => {
    state.routes = routes
}

export const SET_MENULIST = (state, menulist) => {
    state.menulist = menulist
}